import { useGetRepos } from "@/hooks";

const HomePage = () => {
  // const props = useGetRepos()  

  return (
    <div>
      {/* search component */}
      {/* result component */}
    </div>
  );
};

export default HomePage;
